class Solution:
    def firstUniqChar(self, s: str) -> int:
        for i in s:
            a=s.count(i)
            if(a==1):
                return s.index(i)
                break
        else:
                return -1
